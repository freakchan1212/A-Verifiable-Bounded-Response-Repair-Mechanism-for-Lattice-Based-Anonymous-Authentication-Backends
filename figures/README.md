# Figure data and generated outputs

This directory contains the data and generated figures used by the manuscript.

```text
data/ CSV inputs
pdf/ vector PDF outputs
png/ raster preview outputs
svg/ vector SVG outputs
```

The helper script `make_figures.py` is retained for regenerating manuscript figures from the experiment-result bundle when the required result files are available. The already generated figure outputs are included for convenience.
